该应用程序可以不依赖C++ Builder环境来运行。双击即可运行。
也可在C++Builder6.0中打开ExceptionProj.bpr文件，直接编译运行即可。
该程序向读者演示了异常处理的应用。
在测试该程序时，应该运行它编译成功后在相应文件夹的exe文件，才能看到自己定义的异常处理。如果是在C++ Builder环境下运行，该异常会被C++ Builder先捕捉到了，从而看不到自己定义的异常处理。

（注：有时在有的机器上运行时可能会出现一个“Invalid resourec file:....”的警告。
 可以忽略，单击“OK”按钮即可，程序会在编译中重新生成这个文件）
