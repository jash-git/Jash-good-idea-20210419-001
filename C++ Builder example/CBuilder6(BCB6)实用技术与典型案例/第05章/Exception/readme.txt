该应用程序可以不依赖C++ Builder环境来运行。双击即可运行。
也可在C++ Builder 5.0中打开ExceptionProj.bpr文件，直接编译运行即可。
该程序演示了异常处理的应用。
在测试该程序时，应该运行它编译成功后在相应文件夹的.exe文件，才能看到自己定义的异常处理。
如果是在C++ Builder环境下运行，该异常会被C++ Builder先捕捉到了，从而看不到自己定义的异常处理。

