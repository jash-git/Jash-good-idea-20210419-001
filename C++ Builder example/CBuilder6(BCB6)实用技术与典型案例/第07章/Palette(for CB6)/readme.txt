该应用程序可以不依赖C++ Builder环境来运行，双击即可运行。
也可以在C++ Builder 6.0中打开Palette.bpr文件，直接编译运行即可。
该程序将演示采用不同的调色板会对图像的影响。在这里要注意的是要使用256色的位图。
这里是将图像转化为灰度图，如果想要转化为蓝色背景的位图，需要在代码中改变调色板。

（注：有时在有的机器上运行时可能会出现一个“Invalid resourec file:....”的警告。
可以忽略，单击“OK”按钮即可，程序会在编译中重新生成这个文件）