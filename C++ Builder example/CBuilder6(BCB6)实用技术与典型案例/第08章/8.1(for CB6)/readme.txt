这个实时的图像传输程序包含ClientProj和ServerProj两个程序。
先运行ClientProj指定要监控的运端机器的IP；
然后运行ServerProj进行定时截屏或手动截屏。

注意：如果计算机上有防火墙，应该将其关闭。如果是在其他机器上运行，应该单击桌面左下角的“开始”菜单，选择“运行”菜单项，在弹出的对话框中输入Cmd命令，会弹出一个控制台窗口（指Windows 2000，若是Windows 98则直接进入MS-DOS即可），输入IPConfig命令来查询该机的IP，只有输入了正确的IP，程序才能运行。